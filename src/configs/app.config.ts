export const META = Object.freeze({
  title: 'EHRs Patient Encounter',
  url: '/',
  domain: '/',
  author: 'skg',
  date: Date.now()
})

export const appType = {
  USER_DATA: 'USER_DATA'
}

export const Storage = {
  AUTH: 'EHRs_AUTH'
}

export const layoutConfig = {
  theme: 'light', // ? light or dark
  menuLayout: 'horizontal' // ? vertical or horizontal
}